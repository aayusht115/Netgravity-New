# Byte-compiled / optimized / DLL files
__pycache__/
*.pyc
*.pyo
*.pyd

# C extensions
*.so

# Distribution / packaging
.Python
build/
develop-eggs/
dist/
downloads/
eggs/
.eggs/
lib/
!frontend-next/lib/
!frontend-next/lib/*.ts
lib64/
parts/
sdist/
var/
wheels/
share/python-wheels/
*.egg-info/
.installed.cfg
*.egg
MANIFEST

# PyInstaller
#  Usually these files are written by a python script from a template
#  before PyInstaller builds the exe, so as to inject date/other infos into it.
*.manifest
*.spec

# Installer logs
pip-log.txt
pip-delete-this-directory.txt

# Unit test / coverage reports
htmlcov/
.tox/
.nosenv/
.pytest_cache/
.cache/
nosetests.xml
coverage.xml
*.cover
*.py,cover
.flatpak-builder/

# Translations
*.mo
*.pot

# Django stuff:
*.log
local_settings.py
db.sqlite3
db.sqlite3-journal

# Flask stuff:
instance/
.webassets-cache

# Scrapy stuff:
.scrapy

# Sphinx documentation
docs/_build/

# PyBuilder
.pybuilder/
target/

# Jupyter Notebook
.ipynb_checkpoints

# IPython
profile_default/
ipython_config.py

# pyenv
#   For a library or package, you might want to ignore these files since the code is
#   intended to run in multiple environments; otherwise, check them in:
# .python-version

# pipenv
#   According to pypa/pipenv#598, it is recommended to include Pipfile.lock in version control.
#   However, in case of collaboration, if used with a venv, ignore the venv:
# .venv/
# Pipfile.lock

# poetry
#   Similar to Pipfile.lock, it is generally recommended to include poetry.lock in version control.
#   poetry.lock
#   .poetry/

# pdm
#   Similar to Pipfile.lock, it is generally recommended to include pdm.lock in version control.
#   pdm.lock
#   .pdm-plugins/
#   __pypackages__/

# PEP 582; used by e.g. github.com/lincolnloop/layman and github.com/pypa/pipenv
__pypackages__/

# Celery stuff
celerybeat-schedule
celerybeat.pid

# SageMath parsed files
*.sage.py

# Environments
.env
.venv
env/
venv/
ENV/
env.bak/
venv.bak/

# Next.js / Node.js generated files
node_modules/
.next/
*.tsbuildinfo

# Generated by frontend-next/scripts/sync-original-ui.mjs from app/frontend.
frontend-next/public/assets/
frontend-next/public/css/
frontend-next/public/images/
frontend-next/public/js/
frontend-next/public/vendor/
frontend-next/public/netgravity.html

# Spyder project settings
.spyderproject
.spyproject

# VS Code settings
.vscode/

# PyCharm settings
.idea/

# OS-specific files
.DS_Store
.DS_Store?
._*
.Spotlight-V100
.Trashes
ehthumbs.db
Thumbs.db

# NetGravity data zones (Phase 4A — ingestion)
# Raw client files and generated snapshots must never be committed.
# .gitkeep files preserve the folder structure; contents are ignored.
data/raw/*
!data/raw/.gitkeep
data/standardized/*
!data/standardized/.gitkeep
data/curated/*
!data/curated/.gitkeep

# Sample / demo datasets are NOT committed.
# data/mock/india/ is fabricated demo data used to exercise the pipeline.
# It must be shared separately or regenerated before running:
#     python -m netgravity.ingestion --source data/mock/india
data/mock/

# Local transfer scratch — not part of the project
_to_delete/

# Validation-harness RAW per-seed dumps. Regenerable in minutes by running
# `forecasting_validation/run_validation.py` or
# `run_structural_break_validation.py`, and 74k lines of intermediate records
# between them. The SUMMARIES, censuses, benchmarks, plots and reports beside
# them ARE tracked — those are the evidence the docs cite; these are the
# working data the summaries were computed from.
forecasting_validation/**/*_raw.json

# Durable application state (accounts, projects, uploaded networks, scenarios).
# Customer data — never committed. `NETGRAVITY_DB_PATH` selects the location.
data/netgravity.db
data/netgravity.db-wal
data/netgravity.db-shm
*.db
*.db-wal
*.db-shm

# Verified database dumps produced by scripts/backup_database.py
backups/
*.dump
validation/**/backup_check/
